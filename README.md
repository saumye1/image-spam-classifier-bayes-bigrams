Image Spam Classifier Using Naive Bayes' Classifier with Bigrams

Steps to run:

Install tesseract OCR, to extract text from image:
	* sudo apt install tesseract
	
Place all the images in the current directory and run:
	* ./classify_all

